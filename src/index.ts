/**
 * Host half of the file-explorer surface. Registers one HTTP route that lists
 * and reads the current session's workspace files. (Route handler arrives in
 * a later task via TDD; this placeholder keeps the package loadable.)
 */

/** Host plugin body. */
export function apply(): void {}
