node_modules/
*.tsbuildinfo
.npm-cache/

# Local git worktrees (isolated feature workspaces).
.worktrees/

tmp/*

# Agent workflow skills (harness tooling, not project code; live in ~/.dsh/skills).
.dsh/

# Design docs (to be polished and published in a separate repo later).
docs/specs/*
docs/plans/*

# Exploration clones — reference material only, not part of this project.
ref/*
